import React from 'react';

const SwapHistory: React.FC = () => {
  // Placeholder: would load swap history from transaction history service
  return (
    <div className="swap-history">
      <h4>Swap History</h4>
      <div>No swaps recorded yet.</div>
    </div>
  );
};

export default SwapHistory;
